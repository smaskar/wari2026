const W=window.WariData,names={all:'दोन्ही पालख्या',dnyaneshwar:'श्री संत ज्ञानेश्वर महाराज पालखी',tukaram:'जगद्गुरू श्री संत तुकाराम महाराज पालखी'};
let POINTS=W.build(),map,userMarker=null,accCircle=null,markers=[],routeLayer=null,userLocation=null,mukkamRef=null,mukkamName='',mukkamLayer=null,cat='all',docSub='all',ambSub='all',watSub='all',haltSub='all',rad=500,currentPalkhi='all',selectedPoint='';
function $(id){return document.getElementById(id)}function tick(){$('clock').textContent=new Date().toLocaleTimeString('mr-IN',{hour:'numeric',minute:'2-digit'})}tick();setInterval(tick,3e4);
function closeHelpline(){let h=$('helpline');if(h)h.open=false}document.addEventListener('click',e=>{let h=$('helpline');if(h&&h.open&&!h.contains(e.target))h.open=false},true);
function initMap(){if(map)return;if(!window.L){$('map').innerHTML='<div style="padding:18px;font-weight:900">नकाशा ऑफलाइन उपलब्ध नाही. मदत केंद्रांची यादी खाली उपलब्ध आहे.</div>';return}map=L.map('map',{zoomControl:false}).setView([18.41936,74.02765],10);L.control.zoom({position:'bottomleft'}).addTo(map);L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap'}).addTo(map);map.on('popupopen',()=>setTimeout(initLive,60))}
function hav(a,b,c,d){let R=6371000,r=x=>x*Math.PI/180,dl=r(c-a),dn=r(d-b),q=Math.sin(dl/2)**2+Math.cos(r(a))*Math.cos(r(c))*Math.sin(dn/2)**2;return 2*R*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}function fd(m){return m==null?'—':m<1000?Math.round(m)+' मी.':(m/1000).toFixed(1)+' किमी'}function refLoc(){return userLocation||mukkamRef}function dist(p){let r=refLoc();return r?hav(r[0],r[1],p.lat,p.lng):null}function dir(p){return`https://www.google.com/maps/dir/?api=1&destination=${p.lat},${p.lng}`}
const HIRKANI_PHOTO='./assets/img/hirkani-booth.jpg',HIRKANI_VIDEO='./assets/video/hirkani-1.mp4',HIRKANI_VIDEO_LABEL='हिरकणी कक्ष – व्हिडिओ पहा';
function liveBlock(url){let head='<div class="live-head">🔴 थेट प्रक्षेपण · LIVE</div>';if(/youtube\.com|youtu\.be/.test(url)){let id=(url.match(/(?:[?&]v=|youtu\.be\/|\/live\/|\/embed\/)([\w-]{6,})/)||[])[1];let e=id?('https://www.youtube.com/embed/'+id+'?autoplay=1&mute=1&playsinline=1'):url;return`<div class="live-wrap">${head}<iframe class="live-embed" src="${e}" allow="autoplay;encrypted-media;fullscreen;picture-in-picture" allowfullscreen loading="lazy"></iframe></div>`;}return`<div class="live-wrap">${head}<video class="live-embed" data-hls="${W.esc(url)}" controls autoplay muted playsinline></video><div class="live-note">थेट फीड लोड होत नसल्यास पुन्हा प्रयत्न करा</div></div>`;}
function loadHls(cb){if(window.Hls)return cb();let s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/hls.js@1.5.15/dist/hls.min.js';s.onload=cb;s.onerror=()=>{};document.head.appendChild(s);}
function initLive(){document.querySelectorAll('video[data-hls]:not([data-init])').forEach(v=>{v.setAttribute('data-init','1');let url=v.getAttribute('data-hls');if(v.canPlayType('application/vnd.apple.mpegurl')){v.src=url;}else{loadHls(()=>{if(window.Hls&&Hls.isSupported()){let h=new Hls();h.loadSource(url);h.attachMedia(v);}else{v.src=url;}});}});}
function hirkaniMedia(){return`<div class="hk-media"><img src="${HIRKANI_PHOTO}" alt="हिरकणी कक्ष" loading="lazy" onerror="this.remove()"/><div class="hk-vids"><a class="hk-vid" target="_blank" rel="noopener" href="${HIRKANI_VIDEO}">🎬 ${HIRKANI_VIDEO_LABEL}</a></div></div>`}
function inP(p){return currentPalkhi==='all'||p.palkhi===currentPalkhi||p.palkhi==='both'}
function isDocCat(p){return(W.hasHealth(p)||W.hasDoctor(p))&&p.type!=='Ambulance'}
function inC(p,c=cat){if(c==='all')return!W.hasWater(p);return false
||c==='ambulance'&&W.hasAmb(p)&&(ambSub==='all'||ambSub==='als'&&W.isALS(p)||ambSub==='bls'&&W.isBLS(p)||ambSub==='102'&&W.is102(p)||ambSub==='108'&&W.is108(p))
||c==='doc'&&isDocCat(p)&&(docSub==='all'||docSub==='phc'&&W.isPHC(p)||docSub==='rh'&&W.isRuralHospital(p)||docSub==='pvt'&&W.isPrivateHospital(p)||docSub==='hbt'&&W.isHBT(p)||docSub==='icu'&&W.isICU(p))
||c==='water'&&W.hasWater(p)&&(watSub==='all'||watSub==='actual'&&!W.isApprox(p)||watSub==='approx'&&W.isApprox(p))
||c==='toilet'&&W.isToilet(p)
||c==='hirkani'&&W.hasHirkani(p)
||c==='halt'&&W.isHalt(p)&&(haltSub==='all'||haltSub==='mukkam'&&!W.isVisava(p)||haltSub==='visava'&&W.isVisava(p))}
function visN(pred){let a=POINTS.filter(p=>inP(p)&&okSearch(p)&&pred(p));if(refLoc()&&rad<99999999)a=a.filter(p=>{let d=dist(p);return d!=null&&d<=rad});return a.length}
function setDocSub(s){docSub=s;document.querySelectorAll('#docsub .chip').forEach(b=>b.classList.toggle('active',b.dataset.doc===s));refresh()}
function setWatSub(s){watSub=s;document.querySelectorAll('#watsub .chip').forEach(b=>b.classList.toggle('active',b.dataset.wat===s));refresh()}
function setHaltSub(s){haltSub=s;document.querySelectorAll('#haltsub .chip').forEach(b=>b.classList.toggle('active',b.dataset.halt===s));refresh()}
function setAmbSub(s){ambSub=s;document.querySelectorAll('#ambsub .chip').forEach(b=>b.classList.toggle('active',b.dataset.amb===s));refresh()}function okSearch(p){let q=$('search').value.trim().toLowerCase();return!q||[p.label,p.type,p.place,p.vehicle,p.doctor,p.pilot,p.base,p.phase,p.date].join(' ').toLowerCase().includes(q)}
function visible(c=cat){let pts=POINTS.filter(p=>inP(p)&&inC(p,c)&&okSearch(p)).map(p=>({...p,_idx:POINTS.indexOf(p),dist:dist(p)}));if(refLoc()&&rad<99999999)pts=pts.filter(p=>p.dist!=null&&p.dist<=rad);return pts.sort((a,b)=>refLoc()?(a.dist??9e9)-(b.dist??9e9):(a.palkhi+a.type+a.label).localeCompare(b.palkhi+b.type+b.label))}
function counts(){let s=refLoc()?' जवळ':' एकूण';let set=(id,n)=>{let e=$(id);if(e)e.textContent=n+s};
let ap=POINTS.filter(p=>inP(p)&&okSearch(p)&&W.hasAmb(p));if(userLocation&&rad<99999999)ap=ap.filter(p=>{let d=dist(p);return d!=null&&d<=rad});
set('countAmb',W.vehicleCount(ap));set('countDoc',visN(isDocCat));set('countHalt',visN(W.isHalt));set('countHirkani',visN(W.hasHirkani));set('countWater',visN(W.hasWater));
let tp=POINTS.filter(p=>inP(p)&&okSearch(p)&&W.isToilet(p));if(userLocation&&rad<99999999)tp=tp.filter(p=>{let d=dist(p);return d!=null&&d<=rad});
let seats=tp.reduce((n,p)=>n+(parseInt(p.toilet)||0),0);let e=$('countToilet');if(e)e.textContent=seats.toLocaleString('en-IN')+s}
function openMapPanel(){closeHelpline();$('page').classList.add('map-open');initMap();refresh();setTimeout(()=>{if(map){map.invalidateSize(true);fitMappedArea()}$('mapPanel').scrollIntoView({behavior:'smooth',block:'start'})},100)}
function setPalkhi(p){closeHelpline();currentPalkhi=p;document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.p===p));$('palkhiTitle').textContent=names[p];refresh();fitMappedArea();try{parent.postMessage({type:'PALKHI_FILTER',palkhi:p},'*')}catch(e){}}
function chooseHelp(c){closeHelpline();cat=c;document.querySelectorAll('.help,.legend-btn').forEach(b=>b.classList.toggle('active',b.dataset.cat===c));
const SUBS=[['docsub','doc','doc',v=>docSub=v],['ambsub','ambulance','amb',v=>ambSub=v],['watsub','water','wat',v=>watSub=v],['haltsub','halt','halt',v=>haltSub=v]];
SUBS.forEach(([id,forCat,attr,setv])=>{let el=$(id);if(!el)return;el.style.display=c===forCat?'flex':'none';if(c!==forCat){setv('all');el.querySelectorAll('.chip').forEach(b=>b.classList.toggle('active',b.dataset[attr]==='all'))}});
openMapPanel()}function setRadius(r){rad=r;document.querySelectorAll('#rads .chip').forEach(b=>b.classList.toggle('active',+b.dataset.r===r));refresh()}function showMappedArea(){closeHelpline();cat='all';rad=99999999;$('search').value='';document.querySelectorAll('.help').forEach(b=>b.classList.remove('active'));document.querySelectorAll('#rads .chip').forEach(b=>b.classList.toggle('active',+b.dataset.r===99999999));refresh();fitMappedArea()}
function popup(p){let c=W.tel(p.call),chips=W.services(p).map(x=>`<span class="pchip">${W.esc(x)}</span>`).join('');let d=[];if(p.place&&!(p.label||'').includes(p.place))d.push(W.esc(p.place));if(p.vehicle)d.push('🚗 '+W.esc(p.vehicle));if(p.mo)d.push('🧑‍⚕️ '+W.esc(p.mo));if(p.doctor)d.push(W.esc(p.doctor));if(p.pilot)d.push(W.esc(p.pilot));if(p.base)d.push(W.esc(p.base));let hk=W.hasHirkani(p)?`<img class="pop-thumb" src="${HIRKANI_PHOTO}" loading="lazy" onerror="this.remove()"/>`:'';return`<div class="pop"><div class="ptitle">${W.icon(p)} ${W.esc(p.label)}</div><div class="psub">${W.esc(W.NAMES[p.palkhi]||p.palkhi)}</div><div class="pchips">${chips}</div>${p.live?liveBlock(p.live):''}${hk}${d.length?`<div class="pdet">${d.join('<br>')}</div>`:''}<div class="pact">${c?`<a class="pbtn call" href="tel:${c}">📞 फोन</a>`:''}<a class="pbtn" target="_blank" href="${dir(p)}">🧭 दिशा</a>${W.hasHirkani(p)?`<a class="pbtn" href="${HIRKANI_VIDEO}" target="_blank" rel="noopener">🎬</a>`:''}</div></div>`}
function drawRoute(){if(!map)return;if(routeLayer){map.removeLayer(routeLayer);routeLayer=null}
routeLayer=L.layerGroup().addTo(map);
let dny=(window.WARI_DNYANESHWAR_PUNE_ROUTE||[]).concat(window.WARI_ROUTE_DNY_EXT||[]);
let tuk=window.WARI_ROUTE_TUK||[];
if(currentPalkhi!=='tukaram'&&dny.length)L.polyline(dny,{color:'#1d5fc4',weight:4,opacity:.8,lineCap:'round',lineJoin:'round'}).addTo(routeLayer);
if(currentPalkhi!=='dnyaneshwar'&&tuk.length)L.polyline(tuk,{color:'#f4711f',weight:4,opacity:.8,lineCap:'round',lineJoin:'round'}).addTo(routeLayer);
let tr=window.WARI_ROUTE_TUK_RET||[];
if(currentPalkhi!=='dnyaneshwar'&&tr.length)L.polyline(tr,{color:'#f4711f',weight:3,opacity:.55,dashArray:'8 8'}).addTo(routeLayer);}
function draw(pts){if(!map)return;markers.forEach(m=>map.removeLayer(m));markers=[];pts.forEach(p=>{let m=L.marker([p.lat,p.lng],{icon:L.divIcon({className:'',html:`<div class="pin ${W.cls(p)} pal-${p.palkhi}${p.live?' has-live':''}${W.multi(p)?' multi':''}${W.isApprox(p)?' approx':''}">${W.icon(p)}</div>`,iconSize:[30,30],iconAnchor:[15,15]})}).addTo(map).bindPopup(popup(p),{maxWidth:250});markers.push(m)})}
function fitMappedArea(){if(!map)return;let v=visible().filter(p=>isFinite(p.lat)&&isFinite(p.lng)).map(p=>[p.lat,p.lng]);if(v.length){map.fitBounds(L.latLngBounds(v).pad(.12));setTimeout(()=>map.invalidateSize(true),150)}}
function phoneHref(raw){let s=(raw||'').replace(/[^0-9+]/g,'');return s.length===10&&/^[6-9]/.test(s)?'+91'+s:s}function phoneLink(n){n=(n||'').trim();return`<a class="phone-pill" href="tel:${phoneHref(n)}">📞 ${W.esc(n)}</a>`}function splitContacts(v){return(v||'').split(/\s*;\s*/).map(x=>x.trim()).filter(Boolean).map(x=>{let nums=x.match(/(?:\+?91[\s-]?)?[6-9]\d{9}|0\d{2,4}[\s-]?\d{6,8}|1800[\s-]?\d{2,4}[\s-]?\d{3,4}|155388|1075|1077|104|102/g)||[];let name=x.replace(/(?:\+?91[\s-]?)?[6-9]\d{9}|0\d{2,4}[\s-]?\d{6,8}|1800[\s-]?\d{2,4}[\s-]?\d{3,4}|155388|1075|1077|104|102/g,'').replace(/[()]/g,'').replace(/\s+/g,' ').trim();return{name:name||'संपर्क',phone:nums[0]||''}})}
function contactSection(title,val,extra=[]){let arr=[...extra,...splitContacts(val)];if(!arr.length)return'';return`<div class="contact-section"><div class="contact-heading">${W.esc(title)}</div>${arr.map(c=>`<div class="contact-line ${c.cls||''}"><span class="contact-name">${W.esc(c.name)}</span>${c.phone?phoneLink(c.phone):''}</div>`).join('')}</div>`}
function card(p){let c=W.tel(p.call),service=[W.NAMES[p.palkhi],p.mems?`(${p.mems})`:'' ].filter(Boolean).join(' – ');let extra=c&&W.hasAmb(p)?[{name:'रुग्णवाहिका संपर्क',phone:c,cls:'ambulance-main'}]:[];return`<article class="card ${W.cls(p)}"><span class="dist">${fd(p.dist)}</span><div class="formatted-title">${W.esc(W.hasHirkani(p)||W.isToilet(p)||W.isPolice(p)||W.isHalt(p)?p.label:(W.hasAmb(p)?'रुग्णवाहिका सेवा':W.hasHospital(p)?'रुग्णालय सेवा':W.hasDoc(p)?'डॉक्टर / आरोग्य सेवा':'मदत केंद्र')+' – '+p.label.replace(/\b[A-Z]{2}\s*\d{1,2}\s*[A-Z]{1,3}\s*\d{3,4}\b/ig,'').trim())}</div><div class="badges"><span class="badge">${W.esc(W.NAMES[p.palkhi])}</span></div>${p.vehicle?`<div class="formatted-vehicle"><b>वाहन क्रमांक:</b> ${W.esc(p.vehicle)}</div>`:''}<div class="formatted-service"><b>सेवा:</b> ${W.esc(service)}</div>${p.place?`<div class="formatted-field"><b>ठिकाण:</b> ${W.esc(p.place)}</div>`:''}${p.base?`<div class="formatted-field"><b>बेस:</b> ${W.esc(p.base)}</div>`:''}${p.toilet?`<div class="formatted-field"><b>🚻 एकूण शौचालये:</b> ${W.esc(p.toilet)}${p.sites?` <b>· ठिकाणे:</b> ${W.esc(p.sites)}`:''}</div>`:''}${p.mo?contactSection('वैद्यकीय अधिकारी (MO)',p.mo):''}${contactSection(W.isPolice(p)?'पोलीस अधिकारी':'डॉक्टर / EMSO',p.doctor)}${contactSection('पायलट / EA / रुग्णवाहिका संपर्क',p.pilot,extra)}${p.date?`<div class="formatted-field"><b>तारीख:</b> ${W.esc(p.date)}</div>`:''}${p.phase?`<div class="formatted-field"><b>टप्पा:</b> ${W.esc(p.phase)}</div>`:''}${p.live?liveBlock(p.live):''}${W.hasHirkani(p)?hirkaniMedia():''}<div class="act"><a class="dir" target="_blank" href="${dir(p)}">🧭 दिशा</a><button class="share" onclick="openShare(${p._idx})">📤 शेअर</button></div></article>`}
function render(pts){$('list').innerHTML=pts.length?pts.slice(0,100).map(card).join(''):'<div class="card">या फिल्टरमध्ये कोणतेही मदत केंद्र सापडले नाही.</div>';initLive()}
function refresh(){let pts=visible();$('count').textContent=pts.length;$('gps').textContent=userLocation?'अंतरानुसार क्रम':mukkamRef?('मुक्काम '+mukkamName+' पासून अंतर'):'GPS सुरू नाही';drawRoute();draw(pts);render(pts);counts();if(map)setTimeout(()=>map.invalidateSize(true),100)}
function locateMe(){closeHelpline();openMapPanel();if(!navigator.geolocation){$('gps').textContent='या फोनवर स्थान सुविधा उपलब्ध नाही';return}$('gps').textContent='GPS स्थान घेत आहोत…';navigator.geolocation.getCurrentPosition(pos=>{userLocation=[pos.coords.latitude,pos.coords.longitude];mukkamRef=null;mukkamName='';if(mukkamLayer&&map){map.removeLayer(mukkamLayer);mukkamLayer=null}document.querySelectorAll('#mukkams .mchip').forEach(b=>b.classList.remove('active'));rad=99999999;document.querySelectorAll('#rads .chip').forEach(b=>b.classList.toggle('active',+b.dataset.r===99999999));if(map){if(userMarker)map.removeLayer(userMarker);if(accCircle)map.removeLayer(accCircle);accCircle=L.circle(userLocation,{radius:Math.max(pos.coords.accuracy||30,30),className:'accuracy'}).addTo(map);userMarker=L.marker(userLocation,{icon:L.divIcon({className:'',html:'<div class="userpin"></div>',iconSize:[22,22],iconAnchor:[11,11]})}).addTo(map).bindPopup('📍 आपण येथे आहात').openPopup();map.setView(userLocation,14)}refresh();$('gps').textContent=`तुमच्या जवळची मदत · अचूकता ${Math.round(pos.coords.accuracy)} मी.`},err=>{$('gps').textContent=err&&err.code===1?'स्थान परवानगी नाकारली. सेटिंग्जमध्ये परवानगी द्या.':err&&err.code===3?'GPS वेळेत मिळाले नाही. पुन्हा प्रयत्न करा.':'GPS मिळाले नाही. पुन्हा प्रयत्न करा.'},{enableHighAccuracy:true,timeout:15000,maximumAge:20000})}
function openShare(i){selectedPoint=POINTS[i]?.label||'';$('modal').classList.add('open')}function closeModal(e){if(!e||e.target.id==='modal'||e.target.textContent==='रद्द करा')$('modal').classList.remove('open')}function shareCurrentLocation(){let d=$('dindi').value.trim(),issue=$('issue').value,loc=userLocation?`https://www.google.com/maps/search/?api=1&query=${userLocation[0]},${userLocation[1]}`:'स्थान उपलब्ध नाही',msg=`वारी मदत विनंती%0A${encodeURIComponent(issue)}%0A${d?encodeURIComponent('दिंडी/नाव: '+d)+'%0A':''}${selectedPoint?encodeURIComponent('जवळचे मदत केंद्र: '+selectedPoint)+'%0A':''}${encodeURIComponent('माझे स्थान: ')}${encodeURIComponent(loc)}`;if(navigator.share)navigator.share({title:'वारी मदत विनंती',text:decodeURIComponent(msg.replaceAll('%0A','\n'))}).catch(()=>{});else window.open('https://wa.me/?text='+msg,'_blank');closeModal()}
function chooseMukkam(i){closeHelpline();let m=window.WARI_MUKKAMS[i];if(!m)return;
mukkamRef=[m.lat,m.lng];mukkamName=m.n;userLocation=null;rad=5000;
document.querySelectorAll('#rads .chip').forEach(b=>b.classList.toggle('active',+b.dataset.r===5000));
document.querySelectorAll('#mukkams .mchip').forEach((b,j)=>b.classList.toggle('active',j===i));
if(currentPalkhi!==m.pal)setPalkhi(m.pal);
openMapPanel();
if(map){if(mukkamLayer){map.removeLayer(mukkamLayer)}mukkamLayer=L.layerGroup().addTo(map);
L.circle(mukkamRef,{radius:400,color:'#d92b2b',weight:3,fillOpacity:.08}).addTo(mukkamLayer);
map.setView(mukkamRef,13)}
refresh();$('gps').textContent='मुक्काम '+m.n+' ('+(m.d||'तारीख निश्चित नाही')+') — '+m.v;}
function clearMukkam(){mukkamRef=null;mukkamName='';if(mukkamLayer&&map){map.removeLayer(mukkamLayer);mukkamLayer=null}
document.querySelectorAll('#mukkams .mchip').forEach(b=>b.classList.remove('active'));
rad=99999999;document.querySelectorAll('#rads .chip').forEach(b=>b.classList.toggle('active',+b.dataset.r===99999999));refresh();fitMappedArea()}
(function(){let el=$('mukkams');if(!el||!window.WARI_MUKKAMS)return;
let h='<button class="mchip clear" onclick="clearMukkam()">🗺 सर्व मार्ग</button>';
window.WARI_MUKKAMS.forEach((m,i)=>{let ic=m.pal==='tukaram'?'🟠':'🔵';
h+='<button class="mchip" onclick="chooseMukkam('+i+')">'+ic+' '+(m.d?m.d+' · ':'')+m.n+'</button>'});
el.innerHTML=h})();
(function(){try{if(localStorage.getItem('wariLocAsk'))return;}catch(e){return}
let b=document.createElement('div');b.id='locask';
b.innerHTML='<div class="locask-card"><div class="locask-ic">📍</div><b>जवळची मदत दाखवण्यासाठी<br>तुमचे स्थान (GPS) सुरू करा</b><small>रुग्णवाहिका, डॉक्टर, पाणी — तुमच्या अंतरानुसार दिसतील</small><div class="locask-btns"><button class="la-yes" onclick="locAskYes()">📍 स्थान द्या</button><button class="la-no" onclick="locAskNo()">नंतर</button></div></div>';
document.body.appendChild(b)})();
function locAskDone(){try{localStorage.setItem('wariLocAsk','1')}catch(e){}let b=document.getElementById('locask');if(b)b.remove()}
function locAskYes(){locAskDone();locateMe()}
function locAskNo(){locAskDone()}
initMap();refresh();setTimeout(fitMappedArea,300);addEventListener('resize',()=>{if(map)setTimeout(()=>map.invalidateSize(true),150)});
